In `src/pages/Index.tsx`, update the locked-card helper text:

1. Change text from "Complete your Reorientation to continue your formation." to "Complete reorientation to continue your formation"
2. Change color from `text-text-supporting` to `text-primary` (keep existing `opacity: 0.85` so it stays muted under the locked state).

No other changes.